# n8n
n8n docker deploy

Avvio:
```bash
cp .env.template .env
# Modifica .env con valori reali
docker compose up -d
```

Accesso: https://automation.example.com

Logs:
```bash
docker compose logs -f n8n
```

